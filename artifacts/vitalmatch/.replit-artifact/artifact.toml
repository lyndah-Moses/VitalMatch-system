kind = "web"
previewPath = "/"
title = "VitalMatch"
version = "1.0.0"
id = "artifacts/vitalmatch"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18411

[services.development]
run = "pnpm --filter @workspace/vitalmatch run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/vitalmatch", "run", "build" ]
publicDir = "artifacts/vitalmatch/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18411"
BASE_PATH = "/"
